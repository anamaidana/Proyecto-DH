<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head', {pageTitle: 'Meow!' }); %>
        <script src="/js/registerValidation.js" defer></script>
</head>

<body>
    <%- include('../partials/header'); %>
        <main>
            <div class="form-container">
                <form class="register-form" id='register' novalidate action="/users/createUser" method="POST" enctype="multipart/form-data">
                    <h1 class="title seccion">Crear un Usuario</h1>
                    <div class="field">
                        <label for="name">
                        <span>Nombre y Apellido:</span>
                        <strong><abbr title="required">*</abbr></strong>
                    </label>
                        <input type="text" name="name" id="name" title="No válido" pattern="^[a-zA-ZàáâäãåąčćęèéêëėįìíîïłńòóôöõøùúûüųūÿýżźñçčšžÀÁÂÄÃÅĄĆČĖĘÈÉÊËÌÍÎÏĮŁŃÒÓÔÖÕØÙÚÛÜŲŪŸÝŻŹÑßÇŒÆČŠŽ∂ð ,.'-]+$" required minlength="2" value="<%= locals.oldData ? oldData.name : null %>">
                        <span class="error" aria-live="polite"></span>
                        <i class="fas input-icon"></i>
                        <% if ( locals.errors && errors.name) { %>
                            <p class="text-danger">
                                <%= errors.name.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="field">
                        <label for="email">
                        <span>E-mail:</span>
                        <strong><abbr title="required">*</abbr></strong>
                    </label>
                        <input type="email" name="email" id="email" title="Ingresá un email válido" required value="<%= locals.oldData ? oldData.email : null %>">
                        <span class="error" aria-live="polite"></span>
                        <i class="fas fa-check-circle input-icon"></i>
                        <i class="fas fa-exclamation-circle input-icon"></i>
                        <% if ( locals.errors && errors.email) { %>
                            <p class="text-danger">
                                <%= errors.email.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="field">
                        <label for="password">
                        <span>Contraseña:</span>
                        <strong><abbr title="required">*</abbr></strong>
                    </label>
                        <input type="password" name="password" id="password" title="Debe tener mínimo 8 caracteres, incluir una mayúscula, un número y un símbolo" required pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[@.\-_$_!%*¿?&+])[A-Za-z\d@.\-$_!%*¿?&+]{8,32}$" value="<%= locals.oldData ? oldData.password : null %>">
                        <span class="error" aria-live="polite"></span>
                        <i class="fas fa-check-circle input-icon"></i>
                        <i class="fas fa-exclamation-circle input-icon"></i>
                        <% if ( locals.errors && errors.password) { %>
                            <p class="text-danger">
                                <%= errors.password.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="field">
                        <label for="confirmar-contraseña">
                        <span>Confirmar contraseña:</span>
                        <strong><abbr title="required">*</abbr></strong>
                    </label>
                        <input type="password" name="confirmPassword" id="confirmar-contraseña" title="Las contraseñas no coinciden" pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[@$!%*¿?&+])[A-Za-z\d@$!%*¿?&+]{8,32}$" value="<%= locals.oldData ? oldData.confirmPassword : null %>">
                        <span class="error" aria-live="polite"></span>
                        <i class="fas fa-check-circle input-icon"></i>
                        <i class="fas fa-exclamation-circle input-icon"></i>
                        <% if ( locals.errors && errors.confirmPassword) { %>
                            <p class="text-danger">
                                <%= errors.confirmPassword.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="field">
                        <legend>Categoria de permisos</legend>

                        <label for="administrador">
                            <input type="radio" name="rights" id="administrador" value="2" <%= locals.oldData && ("2" === oldData.rights) ? 'checked' : null %>>
                            <span>Administrador</span></label>

                        <label for="usuario">
                            <input type="radio" name="rights" id="usuario" value="1" <%= locals.oldData && ("1" === oldData.rights) ? 'checked' : null %>>
                            <span>Usuario</span>
                            </label>

                        <% if ( locals.errors && errors.rights) { %>
                            <p class="text-danger">
                                <%= errors.rights.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="field">
                        <div class="file-select">Imagen de Perfil:
                            <strong><abbr title="required">*</abbr></strong>
                            <div class="upload-image__label">
                                <label class="subirImagen" for="userAddImage">
                            </div>
                            <input type="file" name="image" id="userAddImage" accept=".jpg, .jpeg, .png, .gif" title="Cargar una imagen" required>
                            <span class="error" aria-live="polite"></span>
                            <% if ( locals.errors && errors.image) { %>
                                <p class="text-danger">
                                    <%= errors.image.msg %>
                                </p>
                                <% } %>
                            </div>
                        <div>
                            <button type="submit" value="Ingresar" class="button button--reg button--smal error-msg button--outline">Crear usuario</button>
                        </div>
                </form>
                </div>
        </main>
        <%- include('../partials/footer'); %>
</body>